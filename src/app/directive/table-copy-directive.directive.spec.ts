import { TableCopyDirectiveDirective } from './table-copy-directive.directive';

describe('TableCopyDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TableCopyDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
